# Asset attribution — `munder_office.json` + `tilesets/office-tileset.png`

These two files are vendored, unmodified, from the [Munder Difflin](https://github.com/chaitanyagiri/munder-difflin)
project (`src/renderer/src/assets/maps/office.tmj` and `src/renderer/src/assets/tilesets/office-tileset.png`),
which in turn vendors them from [`shahar061/the-office`](https://github.com/shahar061/the-office) (project code: ISC).

## IMPORTANT — non-commercial license

The tileset (`tilesets/office-tileset.png`) and the map built on it (`munder_office.json`) are
[LimeZu](https://limezu.itch.io/) pixel-art assets, distributed under the **LimeZu FREE VERSION
license** (see `tilesets/LIMEZUASSETS-LICENSE.txt`):

- ✅ May be used in **non-commercial** projects only.
- ❌ May **not** be used in commercial projects, and may not be resold.

This repository's own `LICENSE` (Apache 2.0) does **not** extend to these two files — they carry
the stricter LimeZu term regardless of the surrounding project's license. **If this project is
ever used commercially, `munder_office.json` and `tilesets/office-tileset.png` must be replaced or
a paid LimeZu license obtained.** This mirrors the same carve-out Munder Difflin's own
`ATTRIBUTION.md` states for the identical files.

The rest of this directory (`office.json`, `tilesets/Modern_Office_16x16.png`,
`tilesets/Room_Builder_Office_16x16.png`, `tilesets/blocks.png`) predates this note and is unrelated
to it — it is this project's original office map, kept in place and unaffected by this addition.
